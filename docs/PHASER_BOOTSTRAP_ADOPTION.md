# M2 contract diff — pinned Yandex/Phaser bootstrap → existing Squishy

**Status:** reviewed implementation checklist, not an assertion that the new Phaser candidate is done. Source: `mini-games-kit@797b5689767e9dc1059514e0446479e054bf1352`, `bootstrap/yandex-phaser/BOOTSTRAP_MANIFEST.json`, `BOOTSTRAP.md`, `src/main.ts`, `src/app/{platform,startup,viewport}.ts`. Squishy baseline: merged M1 `70af43ec0f66d6b4a4dc23784a044ac7d02d4db0`. Product instruction: **retain existing portrait-first layout for engine migration; landscape is separate**. Apply the template contract-by-contract to the existing repo; its generator refuses a nonempty destination and must not overwrite project data.

| Template file / contract | M2 decision | Proof required before default cutover |
| --- | --- | --- |
| `package.json`, `tsconfig.json`, Vite | **Adapt** pinned Phaser 4.2.1 into isolated candidate, retain strict TS/Vite and two existing builds. Never add Phaser to normal Yandex output while the candidate is behind a separate entry. | Candidate build/typecheck; no Phaser in existing release archive until deliberate cutover; retain exact kit pin. |
| `src/main.ts`, `src/game/BootstrapScene.ts` | **Adapt** scene/game lifetime and semantic presentable event. Candidate begins with its own entry; old `src/main.ts` remains untouched. The placeholder scene is not a successful ready signal. | First actual usable Library frame, first correct rendered squishy frame, one `LoadingAPI.ready()`, idempotent teardown, bfcache navigation. |
| `src/app/platform.ts`, `src/app/language.ts` | **Existing equivalent.** Reuse `src/platform/runtime.ts` (Metrica, Yandex/mock, language) and `src/app/bootstrap.ts` V3/save/settings. Never initialize SDK twice or replace storage/ads policy with template defaults. | One runtime per entry, RU/EN, existing V3 ID/key, reward 8→10 and analytics/ad traces. |
| `src/app/startup.ts`, preload/failure | **Adapt.** Existing `src/app/startup.ts` already has production timing; use kit's preload controller only for actual blocking work and signal ready after correct visual presentation. | No invented progress, precise ready once, rejected async work disposed safely, understandable unsupported-WebGL2 screen. |
| `src/app/runtimeImages.ts`, `docs/ASSET_POLICY.md` | **Existing equivalent / adapt on demand.** `src/app/runtimeAssets.ts`, `docs/RUNTIME_ASSETS.md` already contain format selection and Sharp tooling. Do not add network image requests without an actual startup asset. | Existing asset smoke, no late first-frame fetch; AVIF/WebP only for committed image companions. |
| `src/app/viewport.ts` and stock portrait gate | **Adapt mechanism, reject policy.** Preserve current CSS `.sandbox-stage`/`.sandbox-canvas` dimensions, portrait-first mobile and existing short-landscape behavior. **Do not mount `#rotate-gate` or activate orientation blocker**. `BrowserViewportWatcher` is optional for actual visualViewport/resize improvements, not permission for template's 2:1 landscape sizing. | Same 390×844, 1100×760, 844×390 UI composition; correct client→Phaser→normalized UV mapping and DPR, screen rotation recovery, no scrolling regression. |
| `src/style.css`, `index.html` | **Keep existing.** Candidate has distinct HTML entry and minimal candidate-only CSS; never overwrite shipped HTML/CSS/DOM controls. | Visual diff, transparent WebGL2 canvas + original glow/drop-shadow and decorated foreground order. |
| `src/app/debug.ts` | **Adapt** existing debug tools and kit startup diagnostics only in dev/debug; no external fake SDK or secret data in production. | No debug/probe strings in candidate Yandex output, no window-global objects in public. |
| `tools/yandex-audit.mjs`, `docs/RELEASE_CHECKLIST.md`, CI | **Existing equivalent plus candidate check.** Keep `scripts/verify-yandex-dist.mjs`, 5 MiB uncompressed Squishy budget and existing `release:check` / `qa:browser`. Add candidate-only build and QA without expanding default Pages or Yandex routes. | Both existing output archives unchanged until switch; candidate archive `index.html` at root, real hosted Yandex DRAFT and phone test before default switch. |
| `AGENTS.md`, `BOOTSTRAP.md`, `README.md`, `.env.*`, `.gitignore`, `src/vite-env.d.ts`, project decisions | **Already present / adapt migration notes.** Keep scoped Phaser allowance from merged M0. Record intentionally non-template layout, UI, save and audio ownership instead of copying boilerplate docs. | Source-of-truth consistent with actual implementation; no accidental permanent Phaser/raw duplicate physics or second SDK/runtime. |
| `tests/language.test.ts`, other kit smoke | **Existing equivalent + targeted new tests.** Existing RU/EN, startup/Yandex mocks, Save V3 and M1 Playwright suites stay. Candidate tests must actually render/draw/drag and prove teardown, and later full paint/decor. | Browser QA and production Release Check green independently; M0/M1 evidence compared at matched viewports. |

**M2 technical seam:** PR #31's `Extern` shares the WebGL2 context but **copies** springs and requests `alpha:false` / solid backdrop. It is only a reference. Candidate must import merged `SquishSimulation` rather than copy physics, keep `alpha:true`/transparent composition, and explicitly verify Phaser WebGL2 init/API rather than treating the spike's `context as CanvasRenderingContext2D` cast as a stable interface. Preserve existing visible Canvas 2D decor overlays until their lifecycle can be driven by the Phaser update loop (M4); offscreen appearance baking remains acceptable.

**Gate:** a distinct, branch-addressed candidate boot with a real skwish and original layout/portrait policy, testable without changing default `index.html` or production build. A static placeholder and/or Phaser canvas next to a simultaneously running raw-WebGL squish does *not* satisfy M2.
