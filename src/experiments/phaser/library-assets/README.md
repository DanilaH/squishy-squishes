# Library Hall preview assets

Generated files in this directory are derived from owner-uploaded root PNGs by `npm run asset:library` (`scripts/build-library-hall.mjs`). The original art files are preserved. This is a Pages-only, visually unapproved candidate and is not the approved flattened Library v1 master split. Do not replace or conflate the two sets. Source hashes, crop rectangles, output checksums and rights status are recorded in `sources.json`. Do not move it into the Yandex entrypoint before visual and licensing review.
